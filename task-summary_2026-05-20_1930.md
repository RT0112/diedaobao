# 任务总结 - 2026-05-20 19:30

## 目标
双端 URL 统一迁移到 ServerConfig.kt + 编译安装到 K70 + 推送到 GitHub

## 关键步骤

### 1. ServerConfig 双端统一
- **老人端**：创建 `app/src/main/java/com/falldetector/diedaobao/config/ServerConfig.kt`
  - `BASE_URL = "https://6e2b7f6238485d.lhr.life"`
  - `WS_URL`、`FEEDBACK_URL`、`UPLOAD_LOG_URL` 等全部从 `BASE_URL` 派生
- **子女端**：创建 `app/src/main/java/com/familyguardian/app/config/ServerConfig.kt`
  - 同样逻辑，`val` 不用 `const val`（可派生）

### 2. 修改所有引用文件
**老人端（6个文件）**：
- `CloudBaseClient.kt` → `ServerConfig.BASE_URL`
- `WSClient.kt` → `ServerConfig.WS_URL`
- `FeedbackSender.kt` → `ServerConfig.FEEDBACK_URL`
- `ScreenCaptureService.kt` → `ServerConfig.BASE_URL` + `SIGNAL_URL`
- `AppLogger.kt` → `ServerConfig.UPLOAD_LOG_URL`
- `LogUploader.kt` → `ServerConfig.UPLOAD_LOG_URL`

**子女端（5个文件）**：
- `CloudBaseClient.kt` → `ServerConfig.BASE_URL`
- `WSClient.kt` → `ServerConfig.WS_URL`
- `FeedbackSender.kt` → `ServerConfig.FEEDBACK_URL`
- `RemoteAssistManager.kt` → `ServerConfig.REMOTE_ASSIST_URL`
- `AppLogger.kt` → `ServerConfig.UPLOAD_LOG_URL`

### 3. 编译 + 安装
- **老人端**：versionCode 137→138，versionName 0.45.0→0.45.1，编译成功（1m 18s）
- **子女端**：versionCode 21→22，versionName 0.8.1→0.8.2，编译成功（3m 36s）
- 用 adb 安装到 K70（a0c2910e）：
  ```bash
  /Users/zhou/Library/Android/sdk/platform-tools/adb -s a0c2910e install -r <apk-path>
  ```

### 4. Git 提交 + 推送
- **老人端**：commit `61ce810`，push 到 `RT0112/diedaobao.git` main 分支 ✅
- **子女端**：新建仓库 `RT0112/diedaobao-child.git`，commit `78a4b9a`，push 成功 ✅

### 5. 验证 K70 后端
```bash
curl https://6e2b7f6238485d.lhr.life/health
# 返回：{"status":"ok","ws":{"total":1,"elders":1,"guardians":0}}
```
- ✅ K70 后端正常
- ✅ 已有 1 个老人端 WebSocket 连接
- ✅ 数据库正常

## 关键经验

### ✅ 成功
1. **ServerConfig 集中管理 URL**：以后改 URL 只需改 1 个文件
2. **`val` 不用 `const val`**：URL 派生必须用运行时 `val`
3. **编译前递增 versionCode**：否则 Android 不会更新安装
4. **adb 路径**：`/Users/zhou/Library/Android/sdk/platform-tools/adb`
5. **localhost.run 隧道稳定**：`https://6e2b7f6238485d.lhr.life` 一直在线

### ❌ 失败
1. **AppleScript 发微信**：菜单 UI 定位太脆弱，多次失败
   - 错误：`“System Events”遇到一个错误：不能获得“menu bar item "会话" of menu bar 1 of process "WeChat"”。 (-1728)`
   - **解决方案**：告诉用户 APK 路径，让用户手动发

## 待完成
- [ ] 发微信给用户体验（APK 路径已提供）
- [ ] 真机测试双端功能（老人端 + 子女端 + WebSocket 连接）

## 文件清单
- **新建**：双端 `ServerConfig.kt`
- **修改**：11 个 Kotlin 文件（URL 引用替换）
- **编译输出**：
  - `projects/fall-detection-app/app/build/outputs/apk/debug/app-arm64-v8a-debug.apk`（38MB）
  - `projects/family-guardian-app/app/build/outputs/apk/debug/app-arm64-v8a-debug.apk`（17MB）
- **Git**：双端 main 分支已推送

---
**时间**：2026-05-20 19:30 GMT+8  
**状态**：编译+安装+推送完成，等待真机测试
