# 记忆固化 + 子女端APK编译推送

## 目标
用户要求：学会主动记住操作步骤，不要每次都重新摸索或等用户提醒。

## 关键发现
**JDK真实路径**（记忆里之前写错了）：
```
~/jdk-17/jdk-17.0.2.jdk/Contents/Home   ← 真实路径
```
之前写的是 `~/jdk-17.0.2.jdk`，少了 `jdk-17/` 这一层目录。
从 `~/.gradle/daemon/8.5/daemon-4727.out.log` 中的daemon registry确认。

## 操作执行
1. ✅ 从Gradle daemon日志中找到了JAVA_HOME真实路径
2. ✅ 写入MEMORY.md铁律区（修正路径）
3. ✅ 编译子女端：`JAVA_HOME=/Users/zhou/jdk-17/jdk-17.0.2.jdk/Contents/Home ./gradlew assembleDebug --no-daemon --max-workers=1`
4. ✅ BUILD SUCCESSFUL in 1m 54s
5. ✅ pkill -f gradle
6. ✅ adb push APK → adb install -r → 触发MIUI安装器
7. ✅ 写入SOUL.md固化流程（MIUI安装三步、ADB input text技巧、编译推送完整流程）

## 固化内容（SOUL.md新增）
- JDK路径：`/Users/zhou/jdk-17/jdk-17.0.2.jdk/Contents/Home`
- 编译命令完整模板
- MIUI安装三步流程
- ADB input text空格用`%s`的铁律
- Cloudflared DNS失败替代方案

## 结论
JDK路径已确认并固化。子女端APK编译成功并推送K70，等待用户指纹验证完成安装。
