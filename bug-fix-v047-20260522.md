# Bug修复报告 — 跌倒通知数据补全 + 围栏通知修复 (v0.47)

时间：2026-05-22 01:10

## 后端状态
- Mac本地后端 + ngrok隧道正常运行
- 后端已重启，包含最新代码改动

## Bug 1: 跌倒通知数据不全 ✅ 已修复

### 根因（CC发现）
**不在预期的3个文件**，真正bug在 `FallDetectionService.kt` 的 `showFullScreenAlert()` 方法：
- 跌倒检测有两条路径启动ConfirmActivity
- 前台路径：13个字段全齐 ✅
- **锁屏弹窗路径**（最常见场景）：只传5个基础字段 ❌ → 大部分跌倒事件数据丢失

### 修改文件
**老人端 (fall-detection-app):**
- `FallDetector.kt` — FallDetectionResult新增 ffTimeMs/weightedScore/decisionPath/sensorDataJson/feedRate
- `FallDetectionService.kt` — showFullScreenAlert()补全锁屏路径缺失字段(根因修复)
- `ConfirmActivity.kt` — 从Intent读取完整数据，不再硬编码ffDuration=0L
- `CloudBaseClient.kt` — reportFall()新增检测数据参数

**后端 (diedaobao-server):**
- `server.js` — WS推送fall_event包含 ffDuration/physicalScore/weightedScore/decisionPath/sensorDataJson/feedRate

**子女端 (family-guardian-app):**
- `WSClient.kt` — 解析完整检测数据字段
- `FallNotification.kt` — Room实体新增6字段(带defaultValue)
- `HomeFragment.kt` — FallEvent→FallNotification字段映射
- `FeedbackActivity.kt` — feedRate参数补全

## Bug 2: 围栏通知未推送 ✅ 已修复

### 根因（CC发现）
后端 breach_notify 有3个bug：
1. 老人端传userId，后端解构取elderId → 字段名不一致
2. family_bindings表字段是familyId，代码用guardianId → 字段名错误
3. 只推第一个绑定的家属 → 应推所有

### 修改文件
- `server.js` — 修elderId/userId不一致、familyId/guardianId字段名、推所有家属+pushToRoom兜底

## CC使用经验
- CC一次给大任务会超时（>5min），需要拆分
- 拆分方式：按端拆分（老人端→后端→子女端），每次1-2个文件
- CC修完要编译验证，编译错误再让CC修
- adb路径: /Users/zhou/Library/Android/sdk/platform-tools/adb

## 部署状态
- 老人端 v147 ✅ 已安装到K70
- 子女端 v30 ✅ 已安装到K70 + 发微信
- 后端 ✅ 已重启
- git commit ✅ (473dbee)
