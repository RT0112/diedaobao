# 任务总结：跌倒宝 Bug 修复 + 编译环境搭建

**时间**: 2026-05-16 20:00–21:08 GMT+8
**目标**: 修复 Bug1（子女端收不到跌倒通知）+ Bug2（老人端收到协助请求闪退），并搭建编译环境部署修复

---

## ✅ 已完成

### 代码修复（4个文件）
1. **`server.js`** — fall-report 增加备用推送路径（直接查库 push 给家属，不依赖 rooms 状态）；修复 accuracy 解构缺失；修复 location-sync SQL 逗号 bug
2. **`ws.js`** — joinRoom() 双向绑定修复
3. **`HomeFragment.kt`** — `requireContext()` → 防 detach 崩溃；增加 serviceRunning 守卫防止双重启动
4. **`FallDetectionService.kt`** — `AtomicBoolean assistListenerRegistered` 防重复注册

### 编译环境搭建（Intel Mac x64）
1. ✅ **JDK 17** 安装到 `~/Library/Java/zulu17.44.53-ca-jdk17.0.8.1-macosx_x64/`（x64，匹配本机）
2. ✅ **Android SDK** 安装到 `~/Library/Android/sdk/`：
   - `platform-tools`（adb 等）
   - `platforms;android-34`
   - `build-tools;34.0.0`
   - `extras;google;m2repository`
   - `cmdline-tools/latest`（sdkmanager 等）
3. ✅ **Gradle wrapper** 从备份恢复（`gradlew` + `gradle/` 目录）
4. ✅ `local.properties` 指向 `sdk.dir=/Users/zhou/Library/Android/sdk`
5. ✅ `gradle-wrapper.properties` 改为腾讯镜像源

### 项目结构修复
- 从备份复制 `app/` 目录（含 `app/build.gradle.kts` 和 `app/src/main/`）
- `app/src/main/java/` 里已有正确源码结构

---

## ❌ 未完成 / 阻塞中

### 阻塞 1：Gradle 8.5 发行版下载太慢
- `gradle-wrapper.properties` 已改为腾讯镜像，但 Gradle 8.5 bin.zip（~120MB）下载速度极慢
- 之前的 `./gradlew clean assembleDebug` 因下载超时被 SIGTERM

### 阻塞 2：K70 不在线 / IP 无法访问
- MacBook IP: `192.168.4.7`
- K70 未响应 ping（192.168.4.16/19 均不可达，端口 3000/8022 均 closed）
- 无法 SSH 或 SCP 推送修复后的 server.js 到 K70

---

## 🔧 下一步（3 个选项）

### 选项 A：K70 在线时部署服务端修复（最快）
1. K70 开机联网后，告诉我它的 IP
2. 我 SSH（`ssh localhost -p 8022`，先 `adb forward tcp:8022 tcp:8022`）到 K70
3. 推送 `server.js` + `ws.js` 并重启 Node.js 服务
4. 立即测试 Bug1 修复效果

### 选项 B：完成 Android APK 编译
1. 手动下载 Gradle 8.5 发行版到 `~/.gradle/wrapper/dists/gradle-8.5-bin/5t9huq95ubn472n8rpzujfbqh/`
   ```bash
   curl -L -o ~/.gradle/wrapper/dists/gradle-8.5-bin/5t9huq95ubn472n8rpzujfbqh/gradle-8.5-bin.zip \
     "https://mirrors.cloud.tencent.com/gradle/gradle-8.5-bin.zip"
   ```
2. 解压后运行 `export JAVA_HOME=~/Library/Java/zulu17.44.53-ca-jdk17.0.8.1-macosx_x64 && ./gradlew assembleDebug`
3. APK 生成在 `app/build/outputs/apk/debug/`

### 选项 C：手动部署（无需本机编译）
1. 把修改过的 4 个文件内容发给你
2. 你在有 Android SDK 的环境里编译 + 部署到手机
3. 同时把 `server.js` + `ws.js` 手动替换到 K70 上

---

## 📝 关键文件位置

| 文件 | 路径 |
|------|------|
| 修复后 server.js | `~/.qclaw/workspace-x5kuz49xple53hhg/projects/diedaobao-server/server.js` |
| 修复后 ws.js | `~/.qclaw/workspace-x5kuz49xple53hhg/projects/diedaobao-server/ws.js` |
| 修复后 HomeFragment.kt | `~/.qclaw/workspace-x5kuz49xple53hhg/projects/fall-detection-app/app/src/main/java/com/falldetector/diedaobao/ui/HomeFragment.kt` |
| 修复后 FallDetectionService.kt | `~/.qclaw/workspace-x5kuz49xple53hhg/projects/fall-detection-app/app/src/main/java/com/falldetector/diedaobao/service/FallDetectionService.kt` |
| JAVA_HOME | `~/Library/Java/zulu17.44.53-ca-jdk17.0.8.1-macosx_x64` |
| ANDROID_SDK | `~/Library/Android/sdk` |
| Gradle wrapper 镜像 | `mirrors.cloud.tencent.com/gradle/gradle-8.5-bin.zip` |

---

*生成时间: 2026-05-16 21:08 GMT+8*
